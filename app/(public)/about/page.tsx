import React from "react";
import ModuleTeam from "@/components/about/moduleTeam";
interface Props {}

const page = (props: Props) => {
  return (
    <div>
      <ModuleTeam />
    </div>
  );
};

export default page;
